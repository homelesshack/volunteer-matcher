<h1>Need#index</h1>
<p>Find me in app/views/need/index.html.erb</p>
<ul>
  <% @charity.needs.each do |need| %>
    <li><%= link_to need.skill, need %> <%= link_to 'Edit', [:edit, need] %> | <%= link_to 'Destroy', need, method: :delete %></li>
  <% end %>
</ul>
<%= link_to 'Add a need', [:new, @charity, :need ] %>
